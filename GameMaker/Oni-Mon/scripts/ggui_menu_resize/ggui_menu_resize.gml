///ggui_menu_resize(w,h)
function ggui_menu_resize(argument0, argument1) {
	//Resizes the menu region. Useful if you want to dynamically adjust the menu region,
	//but still fill the max possible region with data.
	menu_w = argument0;
	menu_h = argument1;


}
